name: Keep Streamlit alive

on:
  schedule:
    - cron: '*/40 * * * *'
  workflow_dispatch:  # optional: manuell starten

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Curl request
        run: curl -s https://power-price-dashboard.streamlit.app/
